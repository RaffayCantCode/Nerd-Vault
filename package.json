{
  "name": "nerdvault-v2",
  "version": "2.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "node dev-runner.mjs",
    "build": "pnpm run typecheck && pnpm --filter @workspace/nerdvault --filter @workspace/api-server run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm --filter @workspace/nerdvault --filter @workspace/api-server typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
